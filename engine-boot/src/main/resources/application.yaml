
spring:
  profiles:
    active: dev
  application:
    name: workflow-engine

#common config
deploy:
  version: 1.0.0

#mybatis
mybatis:
  mapper-locations: classpath:mapper/*.xml
server:
  port: 8081